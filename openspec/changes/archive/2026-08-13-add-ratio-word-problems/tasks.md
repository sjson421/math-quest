## 1. Ratio story contract

- [x] 1.1 Extend structured ratio data, exhaustive story verification, difficulty-source
  extraction, recorded-output formatting, and synthetic disagreement tests for the
  `ratio-word` operation (under two hours).
- [x] 1.2 Add at least eight fixed ratio story frames plus source-level checks covering every
  frame in both comparison modes, quantity agreement, content limits, and unregistered-bank
  detection (under two hours).

## 2. Ratio word-problem generator

- [x] 2.1 Implement the `ratio-words` generator with seeded frame and comparison selection,
  constructive five-band counts, exact fraction entry, two surviving wall diagnoses, concise
  hints, and worked solutions (under two hours).
- [x] 2.2 Add independent `ratio-words` tests for exact answers, both comparison modes,
  structured story agreement, fraction form, diagnosis values and survival, variety,
  determinism, bounds, and measurable difficulty growth (under two hours).

## 3. Registry and authorities

- [x] 3.1 Register `ratio-words` last in Unit 11, record representative output, and update
  curriculum coverage for its implemented state, unlock order, and 111-skill playable total
  (under two hours).
- [x] 3.2 Mark curriculum row 11.7 playable, update README and roadmap status text, and close
  roadmap item 19 because its final increment has shipped (under two hours).

## 4. Verification

- [x] 4.1 Run strict OpenSpec validation, focused Unit 11/phrasing/coverage/content tests, the
  full test suite, production build, and lint; retain only documented pre-existing warnings
  (under two hours).
- [x] 4.2 Run the real app at 375 pixels using `docs/environment.md`; complete representative
  part-to-part and part-to-whole problems, exercise both diagnosed answers, capture and inspect
  a screenshot, stop temporary services, and confirm their ports are free (under two hours).
