## 1. Structured function support

- [x] 1.1 Add the Unit 19 equation and coordinate operation payloads, then serialize their complete visible source data in recorded output with focused tests.
- [x] 1.2 Extend the exhaustive generator verifier to rebuild Unit 19 notation, sets, linearity, table rules, graph rules, equation rules, comparison winners, choice identities, learner-text collection, and source-magnitude difficulty evidence without importing generator formatting helpers.
- [x] 1.3 Extend the coordinate context and passive plane layout for semantic function tables, accessible equations, and the compact three-form comparison, with focused static-markup and 375-pixel coverage.

## 2. Unit 19 generators

- [x] 2.1 Implement the `function-notation` generator with shuffled reading choices and distinct multiplication and input-output-reversal misconceptions.
- [x] 2.2 Add independent `function-notation` tests covering visible-source agreement, every difficulty, both surviving wall diagnoses, choice-position variation, and representative snapshots.
- [x] 2.3 Implement the `evaluate-function` generator with generated linear rules and exact keypad answers.
- [x] 2.4 Add independent `evaluate-function` tests covering substitution-derived answers, signed draw bands, keypad reachability, every difficulty, and representative snapshots.
- [x] 2.5 Implement the `domain-range` generator with finite plotted functions and canonical domain/range choices.
- [x] 2.6 Add independent `domain-range` tests covering unique inputs, distinct canonical sets, domain/range variation, choice uniqueness, every difficulty, and representative snapshots.
- [x] 2.7 Implement the `linear-vs-nonlinear` generator with unconnected finite-relation points and exact slope classification.
- [x] 2.8 Add independent `linear-vs-nonlinear` tests covering constant and changed rates, accidental-collinearity rejection, both classifications, every difficulty, and representative snapshots.
- [x] 2.9 Implement the `compare-functions` generator with shuffled table, graph, and equation rules and unique rate or initial-value maxima.
- [x] 2.10 Add independent `compare-functions` tests covering three-form source agreement, unique maxima, both requested properties, visible in-bounds graph intercepts for initial-value draws, every winning representation and choice position, every difficulty, and representative snapshots.

## 3. Registry and authorities

- [x] 3.1 Export Unit 19 in manifest order, register all five generators after Unit 18, and update coverage, manifest, and playable-count assertions and snapshots to 173 implemented skills.
- [x] 3.2 Mark all five Unit 19 curriculum rows complete, record increment 19 as shipped, close roadmap item 23, and align the roadmap status text without changing Stage F capabilities.

## 4. Verification

- [x] 4.1 Run the focused Unit 19, coordinate-context, problem-view, recorded-output, generator-verification, coverage, and manifest test files; fix every failure in scope.
- [x] 4.2 Run `npm test`, `npm run build`, and `npm run lint`; accept only the three documented pre-existing `Settings.tsx` warnings.
- [x] 4.3 Follow `docs/environment.md` to exercise all five Unit 19 lessons in the real app at a 375-pixel viewport, confirming answer submission, semantic table/equation/graph presentation, and no horizontal overflow.
