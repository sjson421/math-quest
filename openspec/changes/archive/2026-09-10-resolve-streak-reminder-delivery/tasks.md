## 1. Record the chosen delivery boundary

- [x] 1.1 Update `docs/roadmap.md` increment 31b to state the user-selected in-app-only v1.0 fallback, preserving 31a and the launch exclusions. Leave parent item 31 unchecked until task 2.2 passes. Describe system delivery as out of scope, not shipped or disproven on hardware.
- [x] 1.2 Update README's roadmap/status prose so streak reminders are no longer described as wholly unbuilt: the Home warning ships and no system reminders are sent. Preserve the real-device sync caveat and other unrelated content. Confirm edits are limited to these two documentation files and this task record; no runtime or baseline-spec change is needed.

## 2. Verify the existing reminder and close the item

- [x] 2.1 Run `npx vitest run src/lib/streak.test.ts src/components/StreakCard.test.tsx src/store/progress.test.ts` and record the results here. Check documentation against `openspec/specs/streak-progression/spec.md`, run `openspec validate resolve-streak-reminder-delivery --strict`, and check whitespace with `git diff --check`. Reuse existing tests; add no tests that merely restate unchanged implementation.
  - Passed 2026-09-10: the three existing streak suites passed, 3 files and 139 tests. The roadmap wording remains aligned with the baseline Home warning requirement and its zero-streak/no-motion scenarios. `openspec validate resolve-streak-reminder-delivery --strict` passed, and `git diff --check` reported no whitespace errors.
- [x] 2.2 Perform final real-app browser validation per `docs/environment.md` using an isolated scratch browser profile and disposable IndexedDB fixtures. At 375×812, open Home for a live streak last active yesterday (plain warning visible), a streak already practised today (no at-risk warning), and a fresh zero streak (no loss warning). Also open a stale streak without freezes and confirm load reconciliation shows zero rather than an at-risk live streak. Confirm no notification prompt or reminder opt-in appears on these routes. Capture and inspect a passing screenshot, record the behavior and visual result here, and clean up scratch browser state and any server started for the check. This proves the in-app fallback only, not closed-app delivery or real-device support. Once green, mark roadmap item 31 complete with the actual completion date and recheck the documentation diff for accurate scope and whitespace.
  - Passed 2026-09-10: isolated 375×812 Chromium fixtures showed the plain warning for a live streak last active yesterday, no warning after practice today, no loss warning for zero, and a reconciled zero for a stale streak with no freezes. No notification permission request, prompt, or reminder opt-in appeared. The passing screenshot was inspected: the warning card was fully visible and readable over two lines, with the mascot, controls, goal, and course content aligned without clipping or overlap. Scratch browser profiles and the local Vite server were cleaned up after validation.
