## 1. Form sampling and generators

- [x] 1.1 Add the small Unit 22 form identity/sampling helper using the existing quantitative and algebraic pools. Build one seeded shuffled schedule of exactly 21 quantitative and 25 algebraic sources with replacement, and keep single-problem area selection stateless. Validate the form boundary without importing the registry back into Unit 22. (60–90 minutes)
- [x] 1.2 Add `timed-practice-1` as a source-preserving delegated generator with its own authored teaching line and stable difficulty-1 intro example; register it in existing manifest order. Honor caller-supplied difficulty on isolated generator draws. (45–60 minutes)
- [x] 1.3 Add independent tests for `timed-practice-1`: seeded reproducibility, both pool memberships, no Unit 22 source, complete source representation and answer-form preservation, independent answer recomputation, content contract, intro stability and paired same-source difficulty checks. (60–90 minutes)
- [x] 1.4 Add `timed-practice-2` using the same sampling contract, its own teaching line and stable difficulty-1 example; register it after `timed-practice-1` without promising disjoint papers. (45–60 minutes)
- [x] 1.5 Add independent tests for `timed-practice-2` covering its own seeded outputs, source/answer preservation, independent recomputation, content and teaching-line checks, intro stability and caller-supplied difficulty. (60–90 minutes)
- [x] 1.6 Extend the existing delegated-wrapper test treatment only where needed for the two new ids. Pin exact exclusion sets, retain every source's independent answer and wall-misconception sweeps, and test full schedules for exact 21/25 counts, deterministic order, valid repeated sources and fresh launch sampling. (60–90 minutes)

## 2. Fixed-answer session integration

- [x] 2.1 Extend fixed-answer session construction and materialization in `src/lib/lesson.ts` to consume supplied slot difficulties; retain the existing skip-check adapter's count/difficulty validation. Add the 46-question difficulty-3 form adapter using shared transitions, without standard warm-up, recovery or requeues. (60–90 minutes)
- [x] 2.2 Add pure tests for form construction, invalid source counts, lazy stable materialization, unchanged difficulty after misses, unparseable no-op, wrong-value and wrong-form consumption, 0/46 and 46/46 completion, and rejection of advancement after completion. Keep existing skip-check count/difficulty and standard exact-requeue tests green. (60–90 minutes)

## 3. Individual form practice and credit

- [x] 3.1 Add the explicit form branch at individual-skill lesson entry. Keep review and skip modes explicit and single-problem; retain source identity while choosing the selected form id as the progress owner. Start automatic or already-seen form intros through existing `SkillIntro` behavior without consuming the live seed. (60–90 minutes)
- [x] 3.2 Integrate the form answer flow with existing inputs, feedback and submission gate: hide hints, preserve unfinished-entry correction, count the first scored answer once, advance correct feedback on the existing delay and miss feedback on dismissal, and display consumed questions out of 46. Cancel delayed transitions on leave and guard final completion against duplicate events. (60–90 minutes)
- [x] 3.3 Record ordinary attempts and one ordinary completion only for the form skill, including existing misconception accounting and completion milestones. Compose `ScoreEstimate` with session points out of 46 into the completion surface. Leave source progress unchanged and give no completion reward or score for abandoned forms. (60–90 minutes)
- [x] 3.4 Start the existing elapsed clock at live practice, retain it across intro review and feedback, and stop/discard it at completion or leave. Add concise entry/leave context explaining form size, one scored answer per question, no cutoff and no saved partial paper; keep result and mapping vertically reachable on phones. (45–90 minutes)
- [x] 3.5 Add pure integration-policy and static-markup coverage for form-owner recording, wrong-form outcomes, zero-score completion credit, one completion, stable intros, clock defaults, question counts, absence of hints, and accessible result content. Verify sampled source records stay unchanged and ordinary lessons, review, skip checks and isolated form-generator consumers retain their existing behavior. Use real-browser checks for handlers; do not introduce a DOM test environment. (60–90 minutes)

## 4. Coverage and status documentation

- [x] 4.1 Update registry/manifest coverage to assert all 201 skills implemented, both forms in order, and unchanged graph, quick/wall markers and capabilities. Mark curriculum rows 22.5 and 22.6 complete without changing table structure or skill ids; preserve all earlier completion markers. (45–60 minutes)
- [x] 4.2 Update README, curriculum and roadmap prose for the complete playable course and actual form behavior. Record 30b and parent item 30 complete only after implementation and required verification; retain earlier shipped items and leave item 31 pending. Note that the Unit 22 baseline Purpose must include full-length forms during later archive sync; do not archive during implementation. (30–45 minutes)

## 5. Verification and real-app browser validation

- [x] 5.1 Run focused generator, Unit 22, session, lesson/intro/result and coverage tests, then `npm test`, `npm run build` and `npm run lint`. Run `openspec validate add-stage-h-unit-22b --strict`. Fix only failures within this increment and record results. (45–90 minutes)
- [x] 5.2 Follow `docs/environment.md` using a scratch Playwright script and isolated browser data. Exercise both forms through the real app at 375 by 812 pixels: fresh and seen intros; fixed question total and clock start; intro review preserving current entry/count/time; inherited choice, keypad, fraction/form, expression, coordinate-plane and root-pair controls; wrong, wrong-form and unfinished input; hidden hints; no retry after a scored miss; rapid submit/dismiss; leaving during feedback; fresh restart; completed points/estimate/mapping and ordinary rewards. Verify form-only progress, no completion after abandoned sessions, and no duplicate final reward. Exercise a complete paper for each id, including zero-score and mixed/correct outcomes across browser and pure tests. Check an ordinary lesson, spaced review and skip check remain untimed and use their original pacing, including a form generator as a single source. Check offline results and no horizontal overflow, capture and inspect at least one passing 375-pixel screenshot, record its visual result, and clean only scratch data/server resources created for validation. (90 minutes)
