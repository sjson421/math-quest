## 1. Reconcile roadmap increment 31a

- [x] 1.1 Update only item 31 in `docs/roadmap.md`: label 31a complete through the existing home-screen streak warning; cite `openspec/specs/streak-progression/spec.md`; replace the invitation to expand the nudge with the accepted decision that no additional reminder is required. Preserve the overall unchecked checkbox and 31b's system-notification scope, permission timing, fallback, and real-hardware caveat.

## 2. Verify the accepted behavior and documentation

- [x] 2.1 Check the edited item against `src/components/Home.tsx`, `src/components/StreakCard.tsx`, `src/lib/streak.ts`, and the streak-progression baseline. Confirm it claims only the shipped home-card behavior, leaves 31b outstanding, and introduces no runtime or baseline-spec changes. Run `npx vitest run src/components/StreakCard.test.tsx src/lib/streak.test.ts` and `git diff --check`; retain the results with the task record.

  Evidence: `Home.tsx:201-208` supplies the live streak values to `StreakCard`; `StreakCard.tsx:59-64` renders the warning only for a non-zero at-risk streak; `streak.ts:155-163` defines that condition; and `progress.ts:382-388` reconciles stale streaks on load. The baseline requirement and scenarios at `openspec/specs/streak-progression/spec.md:163-191` match the roadmap wording. The edited item keeps 31b's notification work outstanding and `git diff` shows no runtime or baseline-spec path. `npx vitest run src/components/StreakCard.test.tsx src/lib/streak.test.ts`: 2 files and 52 tests passed. `git diff --check`: passed.
- [x] 2.2 Validate the existing reminder in the real app using `docs/environment.md`: use isolated local progress fixtures for a live streak last practised yesterday, a streak already practised today, and a zero streak; reload each fixture and verify the warning appears only in the first case. Avoid a production recovery key. Check the warning remains legible with reduced motion, capture and inspect a passing screenshot at 375px, then clean up scratch fixtures and any server started for this check. Record results without adding application code or permanent tests.

  Evidence: On 2026-09-10, an isolated Chromium context at 375x812 with `prefers-reduced-motion: reduce` loaded fixtures for a streak last practised yesterday (`warning: true`), a streak already practised today (`warning: false`), and a zero streak (`warning: false`). The warning was visible in the first case. Screenshot captured and inspected at `/tmp/math-quest-browser/streak-warning-375.png`; it remained legible and wrapped without clipping or overlap. The scratch IndexedDB was deleted, the temporary browser context was closed, and the Vite server was stopped after the run. No application code or permanent tests were added.

Specs are explicitly skipped by `.openspec.yaml` because this change alters no behavior. The conditional design artifact is deliberately omitted: this documentation edit has no architectural, dependency, data-model, security, migration, or unresolved technical decision. The existing streak-progression baseline supplies the behavioral reference.
