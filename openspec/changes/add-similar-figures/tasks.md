## 1. Paired geometry support

- [x] 1.1 Extend the closed geometry declaration with the `similar-figures` source fields, strict integer-scale validation, derived measurement labels, accessible name, and two structured proportion references.
- [x] 1.2 Add focused pure tests for both known-side roles, derived labels and formulas, missing-answer omission, and every invalid-data class while proving existing geometry output remains unchanged.
- [x] 1.3 Add one responsive paired-rectangle SVG template with corresponding side letters, three numeric labels, one missing marker, and single-image accessibility.
- [x] 1.4 Add static-markup tests for both missing-side variants, proportion notation, hidden drawing children, responsive classes, and unchanged existing diagram rendering.
- [x] 1.5 Extend learner-text collection, recorded output, unrendered-field checks, difficulty evidence, and global independent geometry verification for the new exhaustive arm.
- [x] 1.6 Add focused gate tests proving complete recording, visible-source answer reconstruction, invalid-scale rejection, and unchanged Unit 20a, Unit 20b, and fraction-diagram output.

## 2. Similar-figures generator

- [x] 2.1 Implement the `similar-figures` generator in under two hours with unequal small sides, growing whole-number scales, both known-side roles, exact keypad answers, two collision-proof diagnoses, and concise teaching content.
- [x] 2.2 Add independent `similar-figures` tests in under two hours for three-side recomputation, both role variants, scale and difficulty growth, surviving diagnoses, formula agreement, unit wording, answer reachability, teaching line, and fixed intro.
- [x] 2.3 Extend Unit 20 recorded snapshots and intro static coverage to the thirteenth example, including both rectangles, side labels, proportions, exact answer, worked steps, and no interactive answer surface.

## 3. Registry and authorities

- [x] 3.1 Append `similar-figures` after `pythagorean`, then update coverage and manifest assertions to pin thirteen implemented Unit 20 ids, nine planned Unit 21 ids, unchanged capabilities and prerequisites, manifest order, and total 186.
- [x] 3.2 Mark curriculum row 20.13 complete, update README and roadmap status text to 186, and record increment 20c as shipped while leaving roadmap item 26 and both Unit 21 increments open.

## 4. Verification

- [x] 4.1 Run focused geometry model, renderer, ProblemView, Unit 20, recorded-output, generator-verification, content-rule, coverage, curriculum-document, and manifest tests; fix every in-scope failure.
- [x] 4.2 Run `npm test`, `npm run build`, and `npm run lint`; accept only explicitly documented pre-existing warnings.
- [x] 4.3 Follow `docs/environment.md` to exercise the `similar-figures` intro and lesson in the real app at 375 by 812 pixels, submit a representative exact answer, assert no overflow or hidden content, capture one passing screenshot, and inspect both rectangles, side labels, missing marker, proportion alignment, and answer spacing.
