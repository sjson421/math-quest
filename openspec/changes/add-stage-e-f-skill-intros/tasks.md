## 1. Unit 12 exponent and root teaching content

- [x] 1.1 Add `exponent-meaning` teaching line: “An exponent tells how many times to use the base as a factor.” (under 2 hours)
- [x] 1.2 Independently pin the exact `exponent-meaning` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and recover the factor count from its visible expansion and `PowerData` without trusting the declared answer (under 2 hours)
- [x] 1.3 Add `evaluate-powers` teaching line: “A power uses its base as a factor as many times as the exponent says.” (under 2 hours)
- [x] 1.4 Independently pin the exact `evaluate-powers` line, require a clean `checkTeachingLine` result, generate its fixed intro example, compute `base ** exponent` from `PowerData`, and retain multiplied-base-by-exponent and swapped-base-and-exponent diagnoses (under 2 hours)
- [x] 1.5 Add `perfect-squares` teaching line: “Squaring a number multiplies it by itself; finding a square root reverses that.” (under 2 hours)
- [x] 1.6 Independently pin the exact `perfect-squares` line, require a clean `checkTeachingLine` result, generate its fixed intro example, and recompute the square or square root from the semantic source value (under 2 hours)
- [x] 1.7 Add `estimate-roots` teaching line: “Compare nearby whole-number squares to find which two the root lies between.” (under 2 hours)
- [x] 1.8 Independently pin the exact `estimate-roots` line, require a clean `checkTeachingLine` result, generate its fixed intro example, and prove the entered whole number and its successor bound the semantic non-square value (under 2 hours)
- [x] 1.9 Add `exponent-multiply` teaching line: “For matching bases multiplied together, add the exponents.” (under 2 hours)
- [x] 1.10 Independently pin the exact `exponent-multiply` line, require a clean `checkTeachingLine` result, generate its fixed intro example, and recompute the missing exponent by adding the two visible semantic exponents (under 2 hours)
- [x] 1.11 Add `exponent-divide` teaching line: “For matching bases divided, subtract the second exponent from the first.” (under 2 hours)
- [x] 1.12 Independently pin the exact `exponent-divide` line, require a clean `checkTeachingLine` result, generate its fixed intro example, and recompute the positive missing exponent by subtracting the semantic exponents in display order (under 2 hours)
- [x] 1.13 Add `power-of-power` teaching line: “When a power is raised again, multiply the two exponents.” (under 2 hours)
- [x] 1.14 Independently pin the exact `power-of-power` line, require a clean `checkTeachingLine` result, generate its fixed intro example, multiply the inner and outer exponents, and retain added-exponents and ignored-outer-exponent diagnoses (under 2 hours)
- [x] 1.15 Add `zero-neg-exponents` teaching line: “A zero exponent gives 1; a negative exponent gives one over the positive power.” (under 2 hours)
- [x] 1.16 Independently pin the exact `zero-neg-exponents` line, require a clean `checkTeachingLine` result, generate its fixed intro example, derive 1 or the exact reciprocal from its semantic family, and check any required fraction and sign entry rules (under 2 hours)
- [x] 1.17 Add `scientific-notation` teaching line: “A positive exponent moves the decimal right; a negative exponent moves it left.” (under 2 hours)
- [x] 1.18 Independently pin the exact `scientific-notation` line, require a clean `checkTeachingLine` result, generate its fixed intro example, derive the exact ordinary number from coefficient, scale, and exponent, and retain both decimal-direction diagnoses (under 2 hours)
- [x] 1.19 Add `pemdas-exponents` teaching line: “Evaluate parentheses first, then powers, multiplication or division, and addition or subtraction.” (under 2 hours)
- [x] 1.20 Independently pin the exact `pemdas-exponents` line, require a clean `checkTeachingLine` result, generate its fixed intro example, and recompute the exact result from its operation-specific semantic expression data (under 2 hours)

## 2. Unit 13 expression teaching content

- [x] 2.1 Add `variable-meaning` teaching line: “A variable is a letter whose value can change.” (under 2 hours)
- [x] 2.2 Independently pin the exact `variable-meaning` line, require a clean `checkTeachingLine` result, generate its fixed intro example, parse the visible coefficient and prompted value, and recompute their product (under 2 hours)
- [x] 2.3 Add `evaluate-expression` teaching line: “Replace the variable with its given value before calculating.” (under 2 hours)
- [x] 2.4 Independently pin the exact `evaluate-expression` line, require a clean `checkTeachingLine` result, generate its fixed intro example, parse its visible term, sign, constant, and prompted value, and recompute the exact result (under 2 hours)
- [x] 2.5 Add `words-to-expression` teaching line: “Translate each phrase in order, but reverse less than and subtracted from.” (under 2 hours)
- [x] 2.6 Independently pin the exact `words-to-expression` line, require a clean `checkTeachingLine` result, generate its fixed intro example, derive the expression from the visible phrase, and retain reversed-order and used-addition diagnoses (under 2 hours)
- [x] 2.7 Add `identify-like-terms` teaching line: “Like terms match in every letter and power.” (under 2 hours)
- [x] 2.8 Independently pin the exact `identify-like-terms` line, require a clean `checkTeachingLine` result, generate its fixed intro example, read the target letter, and resolve the one visible choice carrying the same letter (under 2 hours)
- [x] 2.9 Add `combine-like-terms` teaching line: “Combine like terms by adding their number parts and keeping their letter parts.” (under 2 hours)
- [x] 2.10 Independently pin the exact `combine-like-terms` line, require a clean `checkTeachingLine` result, generate its fixed intro example, parse and add the visible coefficients, preserve the constant, and retain combined-unlike-terms and dropped-variable diagnoses (under 2 hours)
- [x] 2.11 Add `distributive` teaching line: “Distribute by multiplying the outside number by every term inside the brackets.” (under 2 hours)
- [x] 2.12 Independently pin the exact `distributive` line, require a clean `checkTeachingLine` result, generate its fixed intro example, expand its visible factors, and retain both one-term-only diagnoses (under 2 hours)
- [x] 2.13 Add `distribute-negative` teaching line: “A negative outside brackets changes every term's sign when you distribute.” (under 2 hours)
- [x] 2.14 Independently pin the exact `distribute-negative` line, require a clean `checkTeachingLine` result, generate its fixed intro example, expand the visible signed factors, and retain sign-not-flipped, first-term-only, and dropped-outer-sign diagnoses (under 2 hours)
- [x] 2.15 Add `factor-gcf` teaching line: “Take the greatest common factor outside the brackets, leaving each quotient inside.” (under 2 hours)
- [x] 2.16 Independently pin the exact `factor-gcf` line, require a clean `checkTeachingLine` result, generate its fixed intro example, derive the greatest common factor and exact factored form from the visible coefficients, and keep the displayed expansion incorrect (under 2 hours)

## 3. Unit 14 linear-equation teaching content

- [x] 3.1 Add `equation-balance` teaching line: “An equation stays balanced when the same change is made to both sides.” (under 2 hours)
- [x] 3.2 Independently pin the exact `equation-balance` line, require a clean `checkTeachingLine` result, generate its fixed intro example, and recompute both changed sides from the displayed equality and semantic operation (under 2 hours)
- [x] 3.3 Add `one-step-addsub` teaching line: “Undo addition with subtraction, or subtraction with addition, on both sides.” (under 2 hours)
- [x] 3.4 Independently pin the exact `one-step-addsub` line, require a clean `checkTeachingLine` result, generate its fixed intro example, solve the displayed equation from its semantic terms, and retain the repeated-operation diagnosis (under 2 hours)
- [x] 3.5 Add `one-step-multdiv` teaching line: “Undo multiplication with division, or division with multiplication, on both sides.” (under 2 hours)
- [x] 3.6 Independently pin the exact `one-step-multdiv` line, require a clean `checkTeachingLine` result, generate its fixed intro example, solve its multiplication or division equation, and retain the repeated-operation diagnosis (under 2 hours)
- [x] 3.7 Add `two-step` teaching line: “Undo operations in reverse order, doing the same thing to both sides.” (under 2 hours)
- [x] 3.8 Independently pin the exact `two-step` line, require a clean `checkTeachingLine` result, generate its fixed intro example, solve from coefficient, constant, sign, and right side, and retain wrong-order and wrong-sign diagnoses (under 2 hours)
- [x] 3.9 Add `vars-both-sides` teaching line: “Move variable terms to one side before solving.” (under 2 hours)
- [x] 3.10 Independently pin the exact `vars-both-sides` line, require a clean `checkTeachingLine` result, generate its fixed intro example, and solve by independently gathering the two visible variable coefficients and constants (under 2 hours)
- [x] 3.11 Add `equation-parentheses` teaching line: “Distribute first, then gather variable terms and solve.” (under 2 hours)
- [x] 3.12 Independently pin the exact `equation-parentheses` line, require a clean `checkTeachingLine` result, generate its fixed intro example, expand its semantic parenthesized side, and recompute the whole-number solution (under 2 hours)
- [x] 3.13 Add `with-fractions` teaching line: “Multiply every term on both sides by a common denominator before solving.” (under 2 hours)
- [x] 3.14 Independently pin the exact `with-fractions` line, require a clean `checkTeachingLine` result, generate its fixed intro example, clear the displayed semantic denominator, and recompute the exact whole-number solution (under 2 hours)
- [x] 3.15 Add `special-solutions` teaching line: “If the variable disappears, a false statement means no solution; a true one means every solution.” (under 2 hours)
- [x] 3.16 Independently pin the exact `special-solutions` line, require a clean `checkTeachingLine` result, generate its fixed intro example, compare reduced coefficients and constants, and resolve the visible no-, one-, or infinite-solution choice (under 2 hours)
- [x] 3.17 Add `equation-words` teaching line: “Turn the story's steps into an equation, then undo them in reverse order.” (under 2 hours)
- [x] 3.18 Independently pin the exact `equation-words` line, require a clean `checkTeachingLine` result, generate its fixed intro example, derive the solution from carried story terms without trusting the prose or answer, and retain the wrong-order diagnosis (under 2 hours)
- [x] 3.19 Add `rearrange-formula` teaching line: “Move every other term away from the requested letter, then divide by its coefficient.” (under 2 hours)
- [x] 3.20 Independently pin the exact `rearrange-formula` line, require a clean `checkTeachingLine` result, generate its fixed intro example, derive the expanded expression from the displayed two-letter formula, and retain both expression-valued diagnoses (under 2 hours)

## 4. Unit 15 inequality teaching content

- [x] 4.1 Add `inequality-symbols` teaching line: “An inequality shows which side is larger and whether the boundary is included.” (under 2 hours)
- [x] 4.2 Independently pin the exact `inequality-symbols` line, require a clean `checkTeachingLine` result, generate its fixed intro example, derive direction and strictness from the displayed relation, and resolve the matching visible reading choice (under 2 hours)
- [x] 4.3 Add `graph-inequality` teaching line: “Use an open circle for a strict boundary and a closed circle when included.” (under 2 hours)
- [x] 4.4 Independently pin the exact `graph-inequality` line, require a clean `checkTeachingLine` result, generate its fixed intro example, derive circle type and shading direction from the semantic relation, and resolve the matching graph-description choice (under 2 hours)
- [x] 4.5 Add `solve-one-step-ineq` teaching line: “Undo one operation on both sides without changing the inequality sign.” (under 2 hours)
- [x] 4.6 Independently pin the exact `solve-one-step-ineq` line, require a clean `checkTeachingLine` result, generate its fixed intro example, solve its positive one-step relation from semantic values, and retain repeated-operation and needless-reversal diagnoses (under 2 hours)
- [x] 4.7 Add `solve-multi-step-ineq` teaching line: “Undo the constant first, then undo the positive coefficient.” (under 2 hours)
- [x] 4.8 Independently pin the exact `solve-multi-step-ineq` line, require a clean `checkTeachingLine` result, generate its fixed intro example, solve its semantic two-step relation, and retain wrong-order and needless-reversal diagnoses (under 2 hours)
- [x] 4.9 Add `flip-the-sign` teaching line: “Multiplying or dividing both sides by a negative reverses the inequality sign.” (under 2 hours)
- [x] 4.10 Independently pin the exact `flip-the-sign` line, require a clean `checkTeachingLine` result, generate its fixed intro example, divide or multiply by the negative coefficient, reverse the relation, and retain did-not-flip, lost-the-sign, and kept-both diagnoses (under 2 hours)
- [x] 4.11 Add `compound-inequalities` teaching line: “For and, keep values that satisfy both; for or, keep values that satisfy at least one.” (under 2 hours)
- [x] 4.12 Independently pin the exact `compound-inequalities` line, require a clean `checkTeachingLine` result, generate its fixed intro example, count satisfying whole numbers over the stated range, and retain inclusive-boundary and excluded-set diagnoses (under 2 hours)

## 5. Unit 16 coordinate-plane and line teaching content

- [x] 5.1 Add `plot-points` teaching line: “In an ordered pair, the first coordinate moves left or right and the second moves up or down.” (under 2 hours)
- [x] 5.2 Independently pin the exact `plot-points` line, require a clean `checkTeachingLine` result, generate its fixed intro example, recover the target from semantic point data, confirm it is reachable, and retain coordinate-order and vertical-direction diagnoses (under 2 hours)
- [x] 5.3 Add `quadrants` teaching line: “A quadrant is one of four regions named counterclockwise from the upper right.” (under 2 hours)
- [x] 5.4 Independently pin the exact `quadrants` line, require a clean `checkTeachingLine` result, generate its fixed intro example, derive the quadrant from the plotted point's signs, and resolve the matching visible choice (under 2 hours)
- [x] 5.5 Add `table-to-graph` teaching line: “Each table row gives one point to place on the graph.” (under 2 hours)
- [x] 5.6 Independently pin the exact `table-to-graph` line, require a clean `checkTeachingLine` result, generate its fixed intro example, recover the requested point from the semantic table row, and confirm other rows remain pre-plotted (under 2 hours)
- [x] 5.7 Add `slope-from-graph` teaching line: “Slope is vertical change divided by horizontal change between two points.” (under 2 hours)
- [x] 5.8 Independently pin the exact `slope-from-graph` line, require a clean `checkTeachingLine` result, generate its fixed intro example, derive reduced rise over run from the two marked line points, and check required sign and fraction entry (under 2 hours)
- [x] 5.9 Add `slope-from-points` teaching line: “Find slope by subtracting both pairs in the same point order.” (under 2 hours)
- [x] 5.10 Independently pin the exact `slope-from-points` line, require a clean `checkTeachingLine` result, generate its fixed intro example, derive reduced rise over run from the two unjoined points, and retain inconsistent-order and run-over-rise diagnoses (under 2 hours)
- [x] 5.11 Add `y-intercept` teaching line: “The y-intercept is where a line crosses the vertical axis.” (under 2 hours)
- [x] 5.12 Independently pin the exact `y-intercept` line, require a clean `checkTeachingLine` result, generate its fixed intro example, derive the vertical-axis crossing from the line's defining points, and check signed entry when needed (under 2 hours)
- [x] 5.13 Add `slope-intercept` teaching line: “In y = mx + b, m gives rise over run and b gives the vertical crossing.” (under 2 hours)
- [x] 5.14 Independently pin the exact `slope-intercept` line, require a clean `checkTeachingLine` result, generate its fixed intro example, derive both line coefficients from semantic data, and select the one requested by the prompt (under 2 hours)
- [x] 5.15 Add `graph-from-equation` teaching line: “Use b for the vertical crossing, then m for rise over run.” (under 2 hours)
- [x] 5.16 Independently pin the exact `graph-from-equation` line, require a clean `checkTeachingLine` result, generate its fixed intro example, derive each candidate line from its points, and resolve the one matching the visible equation (under 2 hours)
- [x] 5.17 Add `equation-from-graph` teaching line: “Read the vertical crossing and rise over run to write the line's rule.” (under 2 hours)
- [x] 5.18 Independently pin the exact `equation-from-graph` line, require a clean `checkTeachingLine` result, generate its fixed intro example, derive slope and intercept from the rendered line, and rebuild the required expanded expression (under 2 hours)
- [x] 5.19 Add `parallel-perpendicular` teaching line: “Parallel lines keep the same slope; perpendicular lines use its negative reciprocal.” (under 2 hours)
- [x] 5.20 Independently pin the exact `parallel-perpendicular` line, require a clean `checkTeachingLine` result, generate its fixed intro example, derive the reference slope, compute the same or negative-reciprocal slope, and check entry rules (under 2 hours)

## 6. Unit 17 system-of-equations teaching content

- [x] 6.1 Add `system-by-graphing` teaching line: “The point where both lines meet solves both equations.” (under 2 hours)
- [x] 6.2 Independently pin the exact `system-by-graphing` line, require a clean `checkTeachingLine` result, generate its fixed intro example, solve the two rendered lines, and confirm their intersection is the reachable answer point (under 2 hours)
- [x] 6.3 Add `substitution` teaching line: “Replace an isolated letter with its equal expression in the other equation.” (under 2 hours)
- [x] 6.4 Independently pin the exact `substitution` line, require a clean `checkTeachingLine` result, generate its fixed intro example, substitute the isolated semantic equation into the other, and recover the exact ordered pair (under 2 hours)
- [x] 6.5 Add `elimination` teaching line: “Scale every term in one equation, then add or subtract to cancel one letter.” (under 2 hours)
- [x] 6.6 Independently pin the exact `elimination` line, require a clean `checkTeachingLine` result, generate its fixed intro example, solve after scaling the complete selected equation, and retain right-side-not-scaled and eliminate-before-scaling diagnoses (under 2 hours)
- [x] 6.7 Add `system-words` teaching line: “Translate total count and total value into two equations, then solve them together.” (under 2 hours)
- [x] 6.8 Independently pin the exact `system-words` line, require a clean `checkTeachingLine` result, generate its fixed intro example, rebuild both equations from carried pass quantities without trusting prose, and solve the requested ordered pair (under 2 hours)

## 7. Unit 18 polynomial and quadratic teaching content

- [x] 7.1 Add `add-polynomials` teaching line: “Add polynomials by combining number parts on terms with the same power.” (under 2 hours)
- [x] 7.2 Independently pin the exact `add-polynomials` line, require a clean `checkTeachingLine` result, generate its fixed intro example, add matching semantic coefficients, and rebuild the expanded answer (under 2 hours)
- [x] 7.3 Add `sub-polynomials` teaching line: “Subtract a polynomial by changing every term's sign before combining.” (under 2 hours)
- [x] 7.4 Independently pin the exact `sub-polynomials` line, require a clean `checkTeachingLine` result, generate its fixed intro example, subtract matching semantic coefficients, and retain subtracted-first-term-only and added-polynomials diagnoses (under 2 hours)
- [x] 7.5 Add `mult-monomial` teaching line: “Multiply the outside term by every term inside the brackets.” (under 2 hours)
- [x] 7.6 Independently pin the exact `mult-monomial` line, require a clean `checkTeachingLine` result, generate its fixed intro example, multiply visible source coefficients, and rebuild the expanded quadratic (under 2 hours)
- [x] 7.7 Add `foil` teaching line: “Multiply each term in one binomial by each term in the other.” (under 2 hours)
- [x] 7.8 Independently pin the exact `foil` line, require a clean `checkTeachingLine` result, generate its fixed intro example, multiply the two visible binomials, and rebuild all three resulting coefficients (under 2 hours)
- [x] 7.9 Add `factor-gcf-poly` teaching line: “Take the shared number and x outside, then divide each term by both.” (under 2 hours)
- [x] 7.10 Independently pin the exact `factor-gcf-poly` line, require a clean `checkTeachingLine` result, generate its fixed intro example, derive the shared numeric and variable factors, and rebuild the exact factored form (under 2 hours)
- [x] 7.11 Add `factor-trinomial` teaching line: “Find two numbers whose product is the last term and whose sum is the middle number.” (under 2 hours)
- [x] 7.12 Independently pin the exact `factor-trinomial` line, require a clean `checkTeachingLine` result, generate its fixed intro example, derive the unique factor pair from semantic sum and product, and retain product-only and sum-only diagnoses (under 2 hours)
- [x] 7.13 Add `difference-of-squares` teaching line: “Two squares subtracted factor into matching brackets with opposite signs.” (under 2 hours)
- [x] 7.14 Independently pin the exact `difference-of-squares` line, require a clean `checkTeachingLine` result, generate its fixed intro example, take the exact square root of the visible constant, and rebuild both conjugate factors (under 2 hours)
- [x] 7.15 Add `solve-by-factoring` teaching line: “Set each factor equal to zero; every result is a root.” (under 2 hours)
- [x] 7.16 Independently pin the exact `solve-by-factoring` line, require a clean `checkTeachingLine` result, generate its fixed intro example, solve each visible factor for zero, and verify the unordered exact root pair plus both existing diagnoses (under 2 hours)
- [x] 7.17 Add `quadratic-formula` teaching line: “Substitute a, b, and c into the supplied quadratic formula, then use both signs.” (under 2 hours)
- [x] 7.18 Independently pin the exact `quadratic-formula` line, require a clean `checkTeachingLine` result, generate its fixed intro example, derive the discriminant and both exact roots from visible a, b, and c, and retain wrong-sign and wrong-denominator diagnoses (under 2 hours)

## 8. Unit 19 function teaching content

- [x] 8.1 Add `function-notation` teaching line: “Function notation shows an input inside parentheses and its output after the equals sign.” (under 2 hours)
- [x] 8.2 Independently pin the exact `function-notation` line, require a clean `checkTeachingLine` result, generate its fixed intro example, rebuild the visible input-output mapping, and retain function-as-multiplication and input-output-reversed diagnoses (under 2 hours)
- [x] 8.3 Add `evaluate-function` teaching line: “Replace x with the given input, then calculate the output.” (under 2 hours)
- [x] 8.4 Independently pin the exact `evaluate-function` line, require a clean `checkTeachingLine` result, generate its fixed intro example, substitute the semantic input into the visible linear rule, and recompute the exact output (under 2 hours)
- [x] 8.5 Add `domain-range` teaching line: “The domain contains every input; the range contains every distinct output.” (under 2 hours)
- [x] 8.6 Independently pin the exact `domain-range` line, require a clean `checkTeachingLine` result, generate its fixed intro example, derive distinct x- and y-value sets from plotted points, and resolve the requested visible choice (under 2 hours)
- [x] 8.7 Add `linear-vs-nonlinear` teaching line: “A relationship is linear when its rate of change stays constant.” (under 2 hours)
- [x] 8.8 Independently pin the exact `linear-vs-nonlinear` line, require a clean `checkTeachingLine` result, generate its fixed intro example, compare exact consecutive slopes from plotted points, and resolve the linear or nonlinear choice (under 2 hours)
- [x] 8.9 Add `compare-functions` teaching line: “Compare matching rates of change or starting values across all three forms.” (under 2 hours)
- [x] 8.10 Independently pin the exact `compare-functions` line, require a clean `checkTeachingLine` result, generate its fixed intro example, derive the requested property from the table, graph, and equation, and resolve the uniquely greatest representation (under 2 hours)

## 9. Complete teaching-content contract and documentation

- [x] 9.1 After all 62 declarations carry lines, make `teachingLine` required on `SkillConfig` and `SkillGenerator`, keep `defineSkill` as the sole copy boundary, and confirm every existing generator compiles without fallback metadata (under 2 hours)
- [x] 9.2 Derive Stage E and F ids from the manifest, require a teaching line on every playable generator, run authored-source checks for every line, and pin the eight unit-level tracked-vocabulary sequences without changing `VOCABULARY` (under 2 hours)
- [x] 9.3 Update `README.md` and `docs/curriculum.md` to state that all 173 playable skills carry intros, preserving the curriculum tables, counts, and every verbatim skill id (under 2 hours)
- [x] 9.4 Mark roadmap increment 25d and item 25 shipped with the final behavior and required-field decision while leaving item 26 as the first unchecked roadmap item (under 2 hours)

## 10. Automated gates

- [x] 10.1 Run the eight focused Stage E and F unit suites plus curriculum coverage and content-rule suites; fix every scoped failure and confirm recorded-output snapshots remain byte-identical (under 2 hours)
- [x] 10.2 Run `npm test` and fix every scoped failure (under 2 hours)
- [x] 10.3 Run `npm run build` and `npm run lint`; fix every scoped failure and record any verified pre-existing lint output separately (under 2 hours)

## 11. Real-app validation

- [x] 11.1 Follow `docs/environment.md` with a shell-driven Chromium run at 375 by 812 pixels: exercise all 62 Stage E and F intros and assert each exact teaching line, generated example, labelled answer, complete worked steps, and action set has no horizontal or page overflow; explicitly cover notation, equation, coordinate-plane, expression-answer, and root-pair examples; capture and visually inspect a passing `quadratic-formula` intro screenshot; clear test IndexedDB state, stop any server started for the check, and confirm its port is free (under 2 hours)
