## 1. Unit 1 addition teaching content

- [x] 1.1 Add `add-facts-small` teaching line: “Addition combines two amounts into one total.” (under 2 hours)
- [x] 1.2 Independently pin the exact `add-facts-small` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and parse the visible addends and recompute their sum (under 2 hours)
- [x] 1.3 Add `add-facts` teaching line: “Start with the larger number and count on the smaller number.” (under 2 hours)
- [x] 1.4 Independently pin the exact `add-facts` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and parse the visible addends and recompute their sum (under 2 hours)
- [x] 1.5 Add `add-tens` teaching line: “Add the counts of tens, then read the result as tens.” (under 2 hours)
- [x] 1.6 Independently pin the exact `add-tens` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and parse the visible whole tens and recompute their sum (under 2 hours)
- [x] 1.7 Add `add-2digit-nocarry` teaching line: “Line up ones under ones and tens under tens, then add each column.” (under 2 hours)
- [x] 1.8 Independently pin the exact `add-2digit-nocarry` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and sum the visible column operands (under 2 hours)
- [x] 1.9 Add `add-2digit-carry` teaching line: “When the ones total 10 or more, write the ones digit and carry the ten.” (under 2 hours)
- [x] 1.10 Independently pin the exact `add-2digit-carry` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and sum the visible column operands and retain both wall diagnoses (under 2 hours)
- [x] 1.11 Add `add-3digit` teaching line: “Add from right to left, carrying into the next column when needed.” (under 2 hours)
- [x] 1.12 Independently pin the exact `add-3digit` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and sum the visible column operands (under 2 hours)
- [x] 1.13 Add `add-three-numbers` teaching line: “Add all three digits in each column, then carry every full ten.” (under 2 hours)
- [x] 1.14 Independently pin the exact `add-three-numbers` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and sum every visible stacked operand (under 2 hours)
- [x] 1.15 Add `add-words` teaching line: “Find the two amounts being combined, then add them.” (under 2 hours)
- [x] 1.16 Independently pin the exact `add-words` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and add the carried story operands rather than trusting prose or the declared answer (under 2 hours)

## 2. Unit 2 subtraction teaching content

- [x] 2.1 Add `sub-facts-small` teaching line: “Subtraction takes one amount away from another.” (under 2 hours)
- [x] 2.2 Independently pin the exact `sub-facts-small` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and parse the visible operands and recompute their difference (under 2 hours)
- [x] 2.3 Add `sub-facts` teaching line: “Start with the first number and count back the amount taken away.” (under 2 hours)
- [x] 2.4 Independently pin the exact `sub-facts` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and parse the visible operands and recompute their difference (under 2 hours)
- [x] 2.5 Add `sub-tens` teaching line: “Subtract the counts of tens, then read the result as tens.” (under 2 hours)
- [x] 2.6 Independently pin the exact `sub-tens` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and parse the visible whole tens and recompute their difference (under 2 hours)
- [x] 2.7 Add `sub-2digit-noborrow` teaching line: “Line up ones under ones and tens under tens, then subtract each column.” (under 2 hours)
- [x] 2.8 Independently pin the exact `sub-2digit-noborrow` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and subtract the visible column operands (under 2 hours)
- [x] 2.9 Add `sub-2digit-borrow` teaching line: “When the top ones are smaller, trade one ten for 10 ones.” (under 2 hours)
- [x] 2.10 Independently pin the exact `sub-2digit-borrow` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and subtract the visible column operands and retain all three wall diagnoses (under 2 hours)
- [x] 2.11 Add `sub-3digit-borrow` teaching line: “Subtract from right to left, borrowing from the next column when needed.” (under 2 hours)
- [x] 2.12 Independently pin the exact `sub-3digit-borrow` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and subtract the visible column operands (under 2 hours)
- [x] 2.13 Add `sub-across-zero` teaching line: “Borrow through a zero by making 10 tens, then lending one ten onward.” (under 2 hours)
- [x] 2.14 Independently pin the exact `sub-across-zero` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and subtract the visible column operands and retain all three borrow-chain wall diagnoses (under 2 hours)
- [x] 2.15 Add `sub-words` teaching line: “Find the whole and the amount removed, then subtract.” (under 2 hours)
- [x] 2.16 Independently pin the exact `sub-words` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and subtract the carried story operands rather than trusting prose or the declared answer (under 2 hours)

## 3. Unit 3 multiplication teaching content

- [x] 3.1 Extend `TableSkillConfig` with required `teachingLine` metadata and pass it through `tableSkill` to `defineSkill`, leaving the public generator field optional through increment 25c (under 2 hours)
- [x] 3.2 Add `mult-meaning` teaching line: “Multiplication counts equal groups of the same size.” (under 2 hours)
- [x] 3.3 Independently pin the exact `mult-meaning` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and parse the visible factors and recompute their product (under 2 hours)
- [x] 3.4 Add `times-2` teaching line: “Multiplying by 2 doubles the other number.” (under 2 hours)
- [x] 3.5 Independently pin the exact `times-2` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and parse the visible factors and recompute their product (under 2 hours)
- [x] 3.6 Add `times-10` teaching line: “Multiplying by 10 shifts every digit one place left.” (under 2 hours)
- [x] 3.7 Independently pin the exact `times-10` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and parse the visible factors and recompute their product (under 2 hours)
- [x] 3.8 Add `times-5` teaching line: “Five equal groups make half of ten equal groups.” (under 2 hours)
- [x] 3.9 Independently pin the exact `times-5` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and parse the visible factors and recompute their product (under 2 hours)
- [x] 3.10 Add `times-3` teaching line: “Multiplying by 3 adds three equal groups.” (under 2 hours)
- [x] 3.11 Independently pin the exact `times-3` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and parse the visible factors and recompute their product (under 2 hours)
- [x] 3.12 Add `times-4` teaching line: “Multiply by 4 by doubling the number, then doubling again.” (under 2 hours)
- [x] 3.13 Independently pin the exact `times-4` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and parse the visible factors and recompute their product (under 2 hours)
- [x] 3.14 Add `times-6` teaching line: “Six equal groups are five equal groups plus one more.” (under 2 hours)
- [x] 3.15 Independently pin the exact `times-6` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and parse the visible factors and recompute their product (under 2 hours)
- [x] 3.16 Add `times-9` teaching line: “Nine equal groups are ten equal groups minus one group.” (under 2 hours)
- [x] 3.17 Independently pin the exact `times-9` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and parse the visible factors and recompute their product (under 2 hours)
- [x] 3.18 Add `times-7-8` teaching line: “Build seven or eight equal groups from five groups plus the rest.” (under 2 hours)
- [x] 3.19 Independently pin the exact `times-7-8` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and parse the visible factors, recompute their product, and retain both equal-group wall diagnoses (under 2 hours)
- [x] 3.20 Add `times-mixed` teaching line: “Either number can be the group size; the other tells how many groups.” (under 2 hours)
- [x] 3.21 Independently pin the exact `times-mixed` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and parse the visible factors and recompute their product (under 2 hours)
- [x] 3.22 Add `mult-by-10-100` teaching line: “Multiplying by 10 or 100 shifts every digit left one or two places.” (under 2 hours)
- [x] 3.23 Independently pin the exact `mult-by-10-100` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and parse the visible factors and recompute their product (under 2 hours)
- [x] 3.24 Add `mult-2by1` teaching line: “Multiply the ones first, write the ones digit, then multiply tens and add the carry.” (under 2 hours)
- [x] 3.25 Independently pin the exact `mult-2by1` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and multiply the visible column operands and retain both multiplication-carry wall diagnoses (under 2 hours)
- [x] 3.26 Add `mult-2by2` teaching line: “Make one row for each bottom digit, shift the tens row, then add both rows.” (under 2 hours)
- [x] 3.27 Independently pin the exact `mult-2by2` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and multiply the visible column operands and retain both partial-product wall diagnoses (under 2 hours)
- [x] 3.28 Add `mult-words` teaching line: “Use multiplication when equal groups hold the same amount.” (under 2 hours)
- [x] 3.29 Independently pin the exact `mult-words` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and multiply the carried story operands rather than trusting prose or the declared answer (under 2 hours)

## 4. Unit 4 division teaching content

- [x] 4.1 Add `div-meaning` teaching line: “Division shares a total equally or counts equal groups.” (under 2 hours)
- [x] 4.2 Independently pin the exact `div-meaning` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and parse the visible dividend and divisor and recompute the quotient (under 2 hours)
- [x] 4.3 Add `div-facts` teaching line: “Use a multiplication fact backward to find how many groups fit.” (under 2 hours)
- [x] 4.4 Independently pin the exact `div-facts` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and parse the visible dividend and divisor and recompute the quotient (under 2 hours)
- [x] 4.5 Add `div-remainder` teaching line: “A remainder is what stays after making every full group.” (under 2 hours)
- [x] 4.6 Independently pin the exact `div-remainder` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and derive the remainder from the visible dividend and divisor (under 2 hours)
- [x] 4.7 Add `div-by-10-100` teaching line: “Dividing by 10 or 100 shifts every digit right one or two places.” (under 2 hours)
- [x] 4.8 Independently pin the exact `div-by-10-100` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and parse the visible dividend and divisor and recompute the quotient (under 2 hours)
- [x] 4.9 Add `long-div-1digit` teaching line: “Repeat divide, multiply, subtract, and bring down for each digit.” (under 2 hours)
- [x] 4.10 Independently pin the exact `long-div-1digit` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and derive the quotient from the visible division and retain both algorithm-wall diagnoses (under 2 hours)
- [x] 4.11 Add `long-div-remainder` teaching line: “Count only full groups; an unfinished group does not add one.” (under 2 hours)
- [x] 4.12 Independently pin the exact `long-div-remainder` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and derive the whole quotient and leftover from the visible division (under 2 hours)
- [x] 4.13 Add `long-div-2digit` teaching line: “Estimate each quotient digit, multiply to check, then adjust if needed.” (under 2 hours)
- [x] 4.14 Independently pin the exact `long-div-2digit` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and derive the quotient from the visible division and retain both place-sized estimate diagnoses (under 2 hours)
- [x] 4.15 Add `factors` teaching line: “A factor divides a number exactly with nothing left over.” (under 2 hours)
- [x] 4.16 Independently pin the exact `factors` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and derive every factor of the visible number and resolve the matching visible choice label (under 2 hours)
- [x] 4.17 Add `multiples` teaching line: “A multiple comes from multiplying a number by a whole number.” (under 2 hours)
- [x] 4.18 Independently pin the exact `multiples` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and derive the requested multiples of the visible number and resolve the matching visible choice label (under 2 hours)
- [x] 4.19 Add `primes` teaching line: “A prime number can be divided exactly only by 1 and itself.” (under 2 hours)
- [x] 4.20 Independently pin the exact `primes` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and classify the visible number by independent trial division and resolve the matching visible choice label (under 2 hours)
- [x] 4.21 Add `div-words` teaching line: “Find the total and number of equal groups, then divide.” (under 2 hours)
- [x] 4.22 Independently pin the exact `div-words` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and divide the carried story total by its group count rather than trusting prose or the declared answer (under 2 hours)

## 5. Unit 5 order-of-operations teaching content

- [x] 5.1 Add `two-operations` teaching line: “Multiply or divide before adding or subtracting.” (under 2 hours)
- [x] 5.2 Independently pin the exact `two-operations` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and evaluate the visible expression with the independent test evaluator and retain both ordering-wall diagnoses (under 2 hours)
- [x] 5.3 Add `with-parentheses` teaching line: “Work inside parentheses before using operations outside them.” (under 2 hours)
- [x] 5.4 Independently pin the exact `with-parentheses` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and evaluate the visible expression with the independent test evaluator and retain the ignored-parentheses diagnosis (under 2 hours)
- [x] 5.5 Add `pemdas` teaching line: “Use parentheses first, then multiply or divide left to right, then add or subtract left to right.” (under 2 hours)
- [x] 5.6 Independently pin the exact `pemdas` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, and evaluate the visible expression with the independent test evaluator and retain both expression families (under 2 hours)

## 6. Staged coverage and documentation

- [x] 6.1 Derive Stage B ids from the manifest, require teaching lines on exactly Stages A and B, run authored-source checks for every line, and pin Stage B's four tracked Unit 4 vocabulary terms without changing the vocabulary authority (under 2 hours)
- [x] 6.2 Update `README.md` and `docs/curriculum.md`'s enforced-content summary for Stage B intros, preserving the curriculum tables and all 44 verbatim skill ids (under 2 hours)
- [x] 6.3 Mark roadmap increment 25b shipped with its final behavior and decisions while leaving item 25 unchecked for increments 25c and 25d (under 2 hours)

## 7. Automated gates

- [x] 7.1 Run the five focused Stage B unit suites plus curriculum coverage and content-rule suites; fix every scoped failure and confirm recorded-output snapshots remain byte-identical (under 2 hours)
- [x] 7.2 Run `npm test` and fix every scoped failure (under 2 hours)
- [x] 7.3 Run `npm run build` and `npm run lint`; fix every scoped failure and record any verified pre-existing lint output separately (under 2 hours)

## 8. Real-app validation

- [x] 8.1 Follow `docs/environment.md` with a shell-driven Chromium run at 375 by 812 pixels: exercise all 44 Stage B intros and assert each exact teaching line, generated example, labelled answer, complete worked steps, and action set has no horizontal or page overflow; on one representative unseen intro prove leave and return, Start practice, seen-state persistence, Review intro continuity, and feedback exclusivity remain unchanged; capture and visually inspect a passing screenshot; clear test IndexedDB state, stop any server started for the check, and confirm its port is free (under 2 hours)
