## 1. Unit 6 negative-number teaching content

- [x] 1.1 Add `negatives-numberline` teaching line: “Negative numbers sit to the left of zero on a number line.” (under 2 hours)
- [x] 1.2 Independently pin the exact `negatives-numberline` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the visible signed value, derive its exact number-line target, and confirm that target is a declared tick (under 2 hours)
- [x] 1.3 Add `compare-negatives` teaching line: “Farther left on the number line means smaller.” (under 2 hours)
- [x] 1.4 Independently pin the exact `compare-negatives` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, compare the two carried visible values, resolve the matching visible choice label, and retain reversed-comparison and false-equality diagnoses (under 2 hours)
- [x] 1.5 Add `add-neg-pos` teaching line: “With different signs, subtract the sizes and keep the larger size's sign.” (under 2 hours)
- [x] 1.6 Independently pin the exact `add-neg-pos` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the visible operands, recompute their sum, and retain added-magnitudes and wrong-sign diagnoses (under 2 hours)
- [x] 1.7 Add `add-two-negs` teaching line: “Add the sizes of two negative numbers, then keep the negative sign.” (under 2 hours)
- [x] 1.8 Independently pin the exact `add-two-negs` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the visible operands and recompute their sum (under 2 hours)
- [x] 1.9 Add `sub-negatives` teaching line: “Subtracting a negative is the same as adding its positive size.” (under 2 hours)
- [x] 1.10 Independently pin the exact `sub-negatives` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the visible subtraction shape, recompute its value, and retain its still-subtracted plus shape-specific sign diagnoses (under 2 hours)
- [x] 1.11 Add `mult-negatives` teaching line: “When multiplying, matching signs give positive and different signs give negative.” (under 2 hours)
- [x] 1.12 Independently pin the exact `mult-negatives` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the visible factors and recompute their signed product (under 2 hours)
- [x] 1.13 Add `div-negatives` teaching line: “When dividing, matching signs give positive and different signs give negative.” (under 2 hours)
- [x] 1.14 Independently pin the exact `div-negatives` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the visible dividend and divisor and recompute their signed quotient (under 2 hours)
- [x] 1.15 Add `absolute-value` teaching line: “Absolute value is a number's distance from zero.” (under 2 hours)
- [x] 1.16 Independently pin the exact `absolute-value` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the carried visible value and recompute its distance from zero (under 2 hours)
- [x] 1.17 Add `negatives-mixed` teaching line: “Choose the operation first, then apply its negative-number rule.” (under 2 hours)
- [x] 1.18 Independently pin the exact `negatives-mixed` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the fixed example shape selected by the review generator and independently recompute its value (under 2 hours)

## 2. Unit 7 fraction-meaning teaching content

- [x] 2.1 Add `fraction-meaning` teaching line: “A fraction writes selected equal parts over all equal parts.” (under 2 hours)
- [x] 2.2 Independently pin the exact `fraction-meaning` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read selected and total parts from the visible notation and recompute the exact fraction (under 2 hours)
- [x] 2.3 Add `fraction-of-shape` teaching line: “Count shaded equal parts over all equal parts in the shape.” (under 2 hours)
- [x] 2.4 Independently pin the exact `fraction-of-shape` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, derive the exact fraction from the visible diagram counts (under 2 hours)
- [x] 2.5 Add `name-parts` teaching line: “A fraction's top number counts selected parts; its bottom counts all equal parts.” (under 2 hours)
- [x] 2.6 Independently pin the exact `name-parts` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the requested position, independently select its name, and resolve the matching visible choice label (under 2 hours)
- [x] 2.7 Add `fractions-numberline` teaching line: “Split the space from zero to one into equal parts, then count right.” (under 2 hours)
- [x] 2.8 Independently pin the exact `fractions-numberline` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the visible fraction, recompute its exact target, and confirm the target is a declared number-line tick (under 2 hours)
- [x] 2.9 Add `equivalent-visual` teaching line: “Equivalent fractions name the same amount with different equal pieces.” (under 2 hours)
- [x] 2.10 Independently pin the exact `equivalent-visual` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, derive the shaded diagram value and independently select the one visible choice carrying the same rational value (under 2 hours)
- [x] 2.11 Add `equivalent-multiply` teaching line: “Multiply or divide both fraction parts by the same number.” (under 2 hours)
- [x] 2.12 Independently pin the exact `equivalent-multiply` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the visible scale relation and recompute the missing whole number (under 2 hours)
- [x] 2.13 Add `simplify-fractions` teaching line: “Lowest terms use no shared factor except 1.” (under 2 hours)
- [x] 2.14 Independently pin the exact `simplify-fractions` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the visible fraction, reduce it independently by its greatest common factor, check required simplified form, and retain numerator-only and denominator-only diagnoses (under 2 hours)
- [x] 2.15 Add `compare-same-den` teaching line: “With matching denominators, the larger top number makes the larger fraction.” (under 2 hours)
- [x] 2.16 Independently pin the exact `compare-same-den` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, compare the two visible exact fractions and resolve the matching visible choice label (under 2 hours)
- [x] 2.17 Add `compare-diff-den` teaching line: “Rename both fractions with one shared denominator, then compare their top numbers.” (under 2 hours)
- [x] 2.18 Independently pin the exact `compare-diff-den` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, compare the two visible exact fractions, resolve the matching visible choice label, and retain numerator-only and false-equality diagnoses (under 2 hours)

## 3. Unit 8 fraction-operation teaching content

- [x] 3.1 Add `add-frac-same-den` teaching line: “Add the top numbers and keep the shared denominator.” (under 2 hours)
- [x] 3.2 Independently pin the exact `add-frac-same-den` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read both visible fractions, recompute their reduced sum, check required fraction form, and retain adds-denominators and copied-addend diagnoses (under 2 hours)
- [x] 3.3 Add `sub-frac-same-den` teaching line: “Subtract the top numbers and keep the shared denominator.” (under 2 hours)
- [x] 3.4 Independently pin the exact `sub-frac-same-den` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read both visible fractions, recompute their reduced difference, and check required fraction form (under 2 hours)
- [x] 3.5 Add `common-denominator` teaching line: “A common denominator is a shared multiple of both bottom numbers.” (under 2 hours)
- [x] 3.6 Independently pin the exact `common-denominator` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read both visible denominators and recompute their least common multiple (under 2 hours)
- [x] 3.7 Add `add-frac-diff-den` teaching line: “Rename both fractions with a common denominator before adding their top numbers.” (under 2 hours)
- [x] 3.8 Independently pin the exact `add-frac-diff-den` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read both visible fractions, recompute their reduced sum, and retain adds-across and unscaled-numerators diagnoses (under 2 hours)
- [x] 3.9 Add `sub-frac-diff-den` teaching line: “Rename both fractions with a common denominator before subtracting their top numbers.” (under 2 hours)
- [x] 3.10 Independently pin the exact `sub-frac-diff-den` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read both visible fractions and recompute their reduced positive difference (under 2 hours)
- [x] 3.11 Add `improper-to-mixed` teaching line: “Divide an improper fraction's top by its bottom to find the whole and remainder.” (under 2 hours)
- [x] 3.12 Independently pin the exact `improper-to-mixed` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the visible improper fraction, derive the reduced mixed answer, and check required mixed form (under 2 hours)
- [x] 3.13 Add `mixed-to-improper` teaching line: “For a mixed number, multiply the whole by the bottom, then add the top.” (under 2 hours)
- [x] 3.14 Independently pin the exact `mixed-to-improper` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the visible mixed number and derive its reduced improper fraction (under 2 hours)
- [x] 3.15 Add `add-mixed` teaching line: “Add whole parts and fraction parts, then regroup any extra whole.” (under 2 hours)
- [x] 3.16 Independently pin the exact `add-mixed` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read both visible mixed numbers and recompute their reduced mixed sum (under 2 hours)
- [x] 3.17 Add `sub-mixed` teaching line: “Borrow one whole as equal fraction parts before subtracting.” (under 2 hours)
- [x] 3.18 Independently pin the exact `sub-mixed` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read both visible mixed numbers, recompute their reduced difference, and retain reversed-without-borrowing and borrowed-one-piece diagnoses (under 2 hours)
- [x] 3.19 Add `mult-fractions` teaching line: “Multiply top numbers together, bottom numbers together, then reduce.” (under 2 hours)
- [x] 3.20 Independently pin the exact `mult-fractions` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read both visible fractions and recompute their reduced product (under 2 hours)
- [x] 3.21 Add `div-fractions` teaching line: “Keep the first fraction, then multiply by the second fraction's reciprocal.” (under 2 hours)
- [x] 3.22 Independently pin the exact `div-fractions` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read both visible fractions, recompute the quotient, and retain flipped-first and multiplied-without-flip diagnoses (under 2 hours)
- [x] 3.23 Add `fraction-words` teaching line: “Find the named part and whole, then write part over whole.” (under 2 hours)
- [x] 3.24 Independently pin the exact `fraction-words` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, use the carried story part and whole to recompute the exact reduced fraction without trusting the prose or declared answer (under 2 hours)

## 4. Unit 9 decimal teaching content

- [x] 4.1 Add `decimal-place-value` teaching line: “Count places to the right of the decimal point.” (under 2 hours)
- [x] 4.2 Independently pin the exact `decimal-place-value` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the structured visible decimal and requested place and recompute the digit (under 2 hours)
- [x] 4.3 Add `read-decimals` teaching line: “The word "and" marks the decimal point when writing digits.” (under 2 hours)
- [x] 4.4 Independently pin the exact `read-decimals` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the structured value carried with the visible words and recompute its exact decimal (under 2 hours)
- [x] 4.5 Add `compare-decimals` teaching line: “Add ending zeros, then compare matching places from left to right.” (under 2 hours)
- [x] 4.6 Independently pin the exact `compare-decimals` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, compare the two structured visible decimals, resolve the matching visible choice label, and retain longer-means-bigger and false-equality diagnoses (under 2 hours)
- [x] 4.7 Add `round-decimals` teaching line: “Check the next digit: 5 or more rounds up.” (under 2 hours)
- [x] 4.8 Independently pin the exact `round-decimals` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the structured visible decimal and target place and recompute the rounded value (under 2 hours)
- [x] 4.9 Add `add-decimals` teaching line: “Line up decimal points, then add matching places.” (under 2 hours)
- [x] 4.10 Independently pin the exact `add-decimals` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the two visible decimal-column operands and recompute their exact sum (under 2 hours)
- [x] 4.11 Add `sub-decimals` teaching line: “Line up decimal points, then subtract matching places.” (under 2 hours)
- [x] 4.12 Independently pin the exact `sub-decimals` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the two visible decimal-column operands and recompute their exact difference (under 2 hours)
- [x] 4.13 Add `mult-decimals` teaching line: “Multiply as whole numbers, then restore all decimal places.” (under 2 hours)
- [x] 4.14 Independently pin the exact `mult-decimals` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the two visible decimal-column operands, recompute their exact product, and retain both misplaced-point diagnoses (under 2 hours)
- [x] 4.15 Add `div-decimal-by-whole` teaching line: “Divide as whole numbers and bring the decimal point straight up.” (under 2 hours)
- [x] 4.16 Independently pin the exact `div-decimal-by-whole` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the visible decimal dividend and whole divisor and recompute their exact quotient (under 2 hours)
- [x] 4.17 Add `div-by-decimal` teaching line: “Shift both decimal points equally until the divisor is whole.” (under 2 hours)
- [x] 4.18 Independently pin the exact `div-by-decimal` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the two visible decimals, recompute their exact quotient, and retain shifted-divisor-only and shifted-dividend-only diagnoses (under 2 hours)
- [x] 4.19 Add `fraction-to-decimal` teaching line: “Divide the top number by the bottom number to write a decimal.” (under 2 hours)
- [x] 4.20 Independently pin the exact `fraction-to-decimal` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the visible fraction, derive its exact value, and check required decimal form (under 2 hours)
- [x] 4.21 Add `decimal-to-fraction` teaching line: “Write a decimal's digits over their place value, then reduce.” (under 2 hours)
- [x] 4.22 Independently pin the exact `decimal-to-fraction` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the structured visible decimal, derive its reduced exact fraction, and check required fraction form (under 2 hours)
- [x] 4.23 Add `money-problems` teaching line: “Multiply the price by the needed quantity, then write the total in dollars.” (under 2 hours)
- [x] 4.24 Independently pin the exact `money-problems` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, multiply the carried integer-cent price by its carried quantity without trusting prose or the declared answer (under 2 hours)

## 5. Unit 10 percent teaching content

- [x] 5.1 Add `percent-meaning` teaching line: “A percent tells how many parts out of 100.” (under 2 hours)
- [x] 5.2 Independently pin the exact `percent-meaning` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the visible percent or parts-out-of-100 count and recompute the matching whole-number answer (under 2 hours)
- [x] 5.3 Add `percent-to-decimal` teaching line: “Divide a percent by 100, moving the decimal point two places left.” (under 2 hours)
- [x] 5.4 Independently pin the exact `percent-to-decimal` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the visible percent and derive its exact decimal value (under 2 hours)
- [x] 5.5 Add `decimal-to-percent` teaching line: “Multiply a decimal by 100 to write its percent.” (under 2 hours)
- [x] 5.6 Independently pin the exact `decimal-to-percent` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the structured visible decimal, derive the whole-number percent, and retain unmoved-point and one-place-shift diagnoses (under 2 hours)
- [x] 5.7 Add `percent-to-fraction` teaching line: “Write the percent over 100, then reduce the fraction.” (under 2 hours)
- [x] 5.8 Independently pin the exact `percent-to-fraction` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the visible percent, derive its reduced fraction, and check required simplified fraction form (under 2 hours)
- [x] 5.9 Add `percent-of` teaching line: “Multiply the quantity by the percent written as a decimal.” (under 2 hours)
- [x] 5.10 Independently pin the exact `percent-of` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the visible percent and quantity and recompute the exact part (under 2 hours)
- [x] 5.11 Add `find-the-percent` teaching line: “Divide the part by the whole, then multiply by 100.” (under 2 hours)
- [x] 5.12 Independently pin the exact `find-the-percent` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the carried visible part and whole, recompute the percent, and retain unscaled-ratio and reversed-division diagnoses (under 2 hours)
- [x] 5.13 Add `find-the-whole` teaching line: “Divide the part by the percent written as a decimal.” (under 2 hours)
- [x] 5.14 Independently pin the exact `find-the-whole` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the carried visible part and percent, recompute the whole, and retain applied-percent-again and whole-percent diagnoses (under 2 hours)
- [x] 5.15 Add `percent-change` teaching line: “Divide the change by the original amount, then multiply by 100.” (under 2 hours)
- [x] 5.16 Independently pin the exact `percent-change` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the carried original and current values and recompute the positive percent change (under 2 hours)
- [x] 5.17 Add `discount-tax-tip` teaching line: “Find the percent amount, then add or subtract it from the price.” (under 2 hours)
- [x] 5.18 Independently pin the exact `discount-tax-tip` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the carried base cents, percent, and application direction and recompute the exact final money total (under 2 hours)
- [x] 5.19 Add `simple-interest` teaching line: “Use I = Prt, writing the percent rate as a decimal.” (under 2 hours)
- [x] 5.20 Independently pin the exact `simple-interest` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the carried principal, rate, and time and recompute exact interest without adding principal (under 2 hours)

## 6. Unit 11 ratio and proportion teaching content

- [x] 6.1 Add `write-ratios` teaching line: “A ratio compares two amounts in the requested order.” (under 2 hours)
- [x] 6.2 Independently pin the exact `write-ratios` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the carried visible counts and requested order and recompute the directed ratio (under 2 hours)
- [x] 6.3 Add `simplify-ratios` teaching line: “Divide both parts of a ratio by their greatest common factor.” (under 2 hours)
- [x] 6.4 Independently pin the exact `simplify-ratios` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the visible ratio, reduce it independently, and check required lowest-terms fraction form (under 2 hours)
- [x] 6.5 Add `unit-rate` teaching line: “A unit rate compares cost or amount for exactly one item.” (under 2 hours)
- [x] 6.6 Independently pin the exact `unit-rate` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, compute both visible exact prices per item and resolve the unique lower visible choice (under 2 hours)
- [x] 6.7 Add `solve-proportions` teaching line: “In a proportion, cross-multiply, then divide to find the missing value.” (under 2 hours)
- [x] 6.8 Independently pin the exact `solve-proportions` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read all visible ratio terms and recompute the missing whole number by equality (under 2 hours)
- [x] 6.9 Add `scale-drawings` teaching line: “Use the stated scale factor to move between drawing and actual lengths.” (under 2 hours)
- [x] 6.10 Independently pin the exact `scale-drawings` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the carried visible scale, measurement, and direction and recompute the requested length (under 2 hours)
- [x] 6.11 Add `unit-conversion` teaching line: “Multiply toward smaller units and divide toward larger units.” (under 2 hours)
- [x] 6.12 Independently pin the exact `unit-conversion` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, read the stated visible factor, quantity, and direction and recompute the converted amount (under 2 hours)
- [x] 6.13 Add `ratio-words` teaching line: “Decide whether the ratio compares two parts or one part with the whole.” (under 2 hours)
- [x] 6.14 Independently pin the exact `ratio-words` line, require a clean `checkTeachingLine` result, generate its seed-1 difficulty-1 intro example, use carried story counts and comparison type to recompute the directed ratio and retain part-whole confusion and reversed-order diagnoses (under 2 hours)

## 7. Staged coverage and documentation

- [x] 7.1 Derive Stage C and D ids from the manifest, require teaching lines on exactly Stages A–D, run authored-source checks for every line, and pin the six unit-level tracked-vocabulary sequences without changing the vocabulary authority (under 2 hours)
- [x] 7.2 Update `README.md` and `docs/curriculum.md`'s enforced-content summary for all 111 Stage A–D intros, preserving the curriculum tables and every verbatim skill id (under 2 hours)
- [x] 7.3 Correct roadmap increment 25c from 57 to 59 skills and mark it shipped with its final behavior and decisions while leaving item 25 unchecked for increment 25d (under 2 hours)

## 8. Automated gates

- [x] 8.1 Run the six focused Stage C and D unit suites plus curriculum coverage and content-rule suites; fix every scoped failure and confirm recorded-output snapshots remain byte-identical (under 2 hours)
- [x] 8.2 Run `npm test` and fix every scoped failure (under 2 hours)
- [x] 8.3 Run `npm run build` and `npm run lint`; fix every scoped failure and record any verified pre-existing lint output separately (under 2 hours)

## 9. Real-app validation

- [x] 9.1 Follow `docs/environment.md` with a shell-driven Chromium run at 375 by 812 pixels: exercise all 59 Stage C and D intros and assert each exact teaching line, generated example, labelled answer, complete worked steps, and action set has no horizontal or page overflow; start practice from one Stage C intro and prove Review intro returns to the same active problem; exercise both Unit 7 diagram intros; capture and visually inspect a passing diagram screenshot; clear test IndexedDB state, stop any server started for the check, and confirm its port is free (under 2 hours)
